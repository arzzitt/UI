import 'package:flutter/material.dart';

class date3 extends StatelessWidget {
  const date3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.white);
  }
}
